@photoId = 33333333-3333-3333-3333-333333333333

### Get project photos
GET {{baseUrl}}/api/projects/{{projectId}}/photos

### Add project photo (alt defaults to caption if omitted)
POST {{baseUrl}}/api/projects/{{projectId}}/photos
Content-Type: application/json

{
  "uri": "https://example.com/cover.jpg",
  "caption": "Обложка свитера",
  "role": "COVER"
}
> {% client.global.set("photoId", response.body.id); %}

### Add project gallery photo
POST {{baseUrl}}/api/projects/{{projectId}}/photos
Content-Type: application/json

{
  "uri": "https://example.com/gallery-1.jpg",
  "caption": "В процессе",
  "alt": "Фото процесса вязания",
  "role": "GALLERY"
}
> {% client.global.set("photoId", response.body.id); %}

### Get log entry photos
GET {{baseUrl}}/api/projects/{{projectId}}/log/{{logEntryId}}/photos

### Add log entry photo (alt defaults to caption if omitted)
POST {{baseUrl}}/api/projects/{{projectId}}/log/{{logEntryId}}/photos
Content-Type: application/json

{
  "uri": "https://example.com/progress.jpg",
  "caption": "Рукав готов на 70%",
  "role": "GALLERY"
}
> {% client.global.set("photoId", response.body.id); %}

### Patch photo (caption/alt/sortOrder/role/uri)
PATCH {{baseUrl}}/api/photos/{{photoId}}
Content-Type: application/json

{
  "caption": "Новая подпись",
  "alt": "Альт-текст для доступности",
  "sortOrder": 10
}

### Patch photo (set as cover)
PATCH {{baseUrl}}/api/photos/{{photoId}}
Content-Type: application/json

{
  "role": "COVER"
}

### Delete photo
DELETE {{baseUrl}}/api/photos/{{photoId}}
